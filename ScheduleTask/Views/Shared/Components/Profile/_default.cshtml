@model ScheduleTask.Models.Components.ProfileViewModel

<div class="user-panel">
    <div class="pull-right image">
        <img src="@Url.Action("ShowImage","User",new{id=Model.Id})" class="img-circle"
             alt="User Image" asp-append-version="true">
    </div>
    <div class="pull-right info">
        <p>@Model.FuulName</p>
        <a href="#"><i class="fa fa-circle text-success"></i> آنلاین</a>
    </div>
</div>

